import React from 'react'

export default function SectionDash({children}) {
  return (
    <section className='flex items-center justify-center min-h-screen w-screen relative  mb-10 pb-10'>{children}</section>
  )
}
