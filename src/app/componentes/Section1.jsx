import React from 'react'

export default function Section1({children}) {
  return (
    <section className="min-h-screen w-screen flex text-primary-textGris items-center justify-center mx-auto">{children}</section>
  )
}
